/** Shared styling for native selects so they match the Input system. */
export const selectClassNames = [
  'w-full h-12 px-3.5 text-[15px] text-ink appearance-none bg-no-repeat',
  'bg-surface border border-border-default rounded-radius-md cursor-pointer',
  'shadow-[inset_0_1px_2px_rgba(28,27,25,0.04)]',
  'outline-none transition-all hover:border-border-strong focus:border-accent-600 focus-ring',
].join(' ');
